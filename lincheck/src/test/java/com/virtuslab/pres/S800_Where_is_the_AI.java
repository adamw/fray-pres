package com.virtuslab.pres;

public class S800_Where_is_the_AI {
    // Feedback loops are critical ->
    // Fast tests are even more important ->
    // AI agents can now have tools to test concurrent code

    // But: they must be told to do so.

    // + Fray ships with an MCP server, that lets an LLM orchestrate the scheduling
}

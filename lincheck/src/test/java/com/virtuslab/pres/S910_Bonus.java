package com.virtuslab.pres;

public class S910_Bonus {
    void _1_jcstress() {
        /*
        ℹ️ complementary to fray/lincheck
        ℹ️ stress testing using forked JVMs
        ℹ️ small tests with @Actor-s and @Arbiter-s
        ℹ️ parallelism, not concurrency bugs
        🍀 catches parallelsim / memory-model bugs (e.g. instruction reordering)
        🍀 no instrumentation
        💔 can't reuse existing test cases
        💔 non-deterministic, no reproduction of bugs
         */
    }
}
